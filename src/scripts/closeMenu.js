export function closeMenu(button, menu, entry, backdrop) {
  button.classList.remove('change');
  menu.classList.remove('change');
  entry.classList.remove('change');
  backdrop.classList.remove('change');
}